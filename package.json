{
  "name": "agent-mcp",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "deploy": "serverless deploy",
    "deploy:npx": "npx serverless deploy"
  },
  "devDependencies": {
    "serverless": "^4.20.2",
    "serverless-python-requirements": "^5.1.0"
  }
}
